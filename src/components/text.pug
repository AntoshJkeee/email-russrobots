mixin text(props)
	//- подключение проверки пропросов
	include ../shared/shortcut-check-store

	mj-text(font-size=STYLE('fs') font-weight=STYLE('fw') line-height=STYLE('lh') color=STYLE('color'))
		| !{DATA('text')}